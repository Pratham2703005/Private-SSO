'use server';

// Direct exports to ensure 'use server' is at module top level
export { startAuth } from './start-auth';
export { handleCallback } from './callback';
export { validateSession } from './validate-session';
